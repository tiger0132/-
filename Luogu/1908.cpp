#include <bits/stdc++.h>
using namespace std;

struct node {
	int x, y;
	bool operator<(const node& rhs) const {
		if (x == rhs.x) return y < rhs.y;
		return x > rhs.x;
	}
} src[40004];

int rec[40004];
int n;

void add(int i, int x) {
	while (i <= n) 
}

int main() {

}