#include <bits/stdc++.h>
using namespace std;

int n;

void dfs(int i, int j, int len) {
	s
	if (i + (4 - j) > len) {
		return;
	}
	if (i + 3 * (4 - j) < len) {
		return;
	}
	if () {
		
	}
}

void go(int ) {
	
}

int main() {
	cin >> n;
	for (int i = 0; i < n; i++) {
		cin >> 
	} 
}

